##### CSS 变量的定义和使用方法：

```css
/* CSS 变量 */
:root {
	/* 在 :root 里定义的变量，可以在其他所有选择器中使用 */
	/* 命名规则： --xx-xxx-xxx... */
	/* 修改变量值，所有用到这个变量的地方都会统一修改 */
	--main-color: #ff6347;
}
h1 {
	/* var() 获取变量的值 */
	color: var(--main-color);
}
```
