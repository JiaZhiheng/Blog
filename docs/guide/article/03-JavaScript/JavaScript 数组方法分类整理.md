```javascript
const arr = [4, 3, 2, 1];

/* --- 操作数组 --- */

// 在末尾添加元素，结果: [4, 3, 2, 1, 5]
arr.push(5)

// 在开头添加元素，结果: [0, 4, 3, 2, 1, 5]
arr.unshift(0)

// 移除最后一个元素，结果: [0, 4, 3, 2, 1]
arr.pop()

// 移除第一个元素，结果: [4, 3, 2, 1]
arr.shift()

// 移除最后一个元素，结果: [1, 2, 3, 4]
arr.reverse()
```
